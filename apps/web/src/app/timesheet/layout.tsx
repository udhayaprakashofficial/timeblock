export const metadata = {
  title: 'Timesheet',
  description:
    'Review past days and download a timesheet CSV or PDF for your team lead.',
};

export default function TimesheetLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
