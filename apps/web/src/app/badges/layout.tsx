export const metadata = {
  title: 'Effort badges',
  description: 'Unlock daily, consistency, focus, and insight challenges.',
};

export default function BadgesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
