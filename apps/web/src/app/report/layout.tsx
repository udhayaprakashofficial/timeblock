export const metadata = {
  title: 'Weekly report',
  description: 'Estimated vs actual hours, completion, and end-of-day sheet.',
};

export default function ReportLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
