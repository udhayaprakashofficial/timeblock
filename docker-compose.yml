services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: timeblock
      POSTGRES_PASSWORD: timeblock
      POSTGRES_DB: timeblock
    volumes:
      - timeblock_pg:/var/lib/postgresql/data

volumes:
  timeblock_pg:
